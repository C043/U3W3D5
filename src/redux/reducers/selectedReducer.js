const initialState = {
  content: {},
};

const selectedReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default selectedReducer;
